# Compiled per-ABI Rust libraries — rebuilt by `pnpm ubrn:android` (WSL2 on this machine, task 017's
# native-Windows finding). Binaries, not source; never committed, exactly like apps/mobile/android/.
android/src/main/jniLibs/
