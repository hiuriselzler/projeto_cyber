{
  "$comment": "The API's login password hash (04 §2). The app's privacy-key wrapping KDF may never be cheaper: its memory and iterations are each at least these (ADR-007). The API's test holds its hasher equal to this file; the app's test holds its KDF at or above it.",
  "login_hash": {
    "algorithm": "argon2id",
    "memory_kib": 65536,
    "iterations": 3,
    "parallelism": 4
  }
}
