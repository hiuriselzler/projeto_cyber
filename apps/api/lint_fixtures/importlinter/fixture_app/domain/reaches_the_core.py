# The allowed side of "Only the domain reaches the core": this import is legal, and the test asserts
# it is reported against no contract. A fence that also caught the one caller it exists to permit
# would be indistinguishable from a broken one.
import cyberathlete_core
