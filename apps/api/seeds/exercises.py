"""Seeds the global exercise catalog: `uv run python -m seeds.exercises` (06 §1).

The catalog is task 002's work; until then this is a no-op, so the documented command still runs.
"""


def main() -> None:
    print("No seed data yet: the exercise catalog arrives in task 002.")


if __name__ == "__main__":
    main()
