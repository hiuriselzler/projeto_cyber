import { Screen } from '@/ui';

/**
 * What a release build shows until the Today screen exists (task 010): the theme's ground and nothing else. It carries
 * no text, so there is nothing to translate yet (INV-27).
 */
export function HomeScreen() {
  return <Screen />;
}
